# Hook Modes

`handoff --mode summary`
- Default context transfer from source to target agent.

`handoff --mode bugfix`
- Formats source context as a bugfix directive for the target agent.

`handoff --mode review`
- Formats source context as a structured review request.
